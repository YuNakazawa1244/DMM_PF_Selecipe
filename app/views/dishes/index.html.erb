<h1>dishのindexページです</h1>

<div class="container px-5 px-sm-0">
　<div class="row">
　　<div class="col-md-3">
　　　　<%#= render 'books/userinfo', user: current_user,book: @new_book %>
　　</div>
    <div class="col-md-7 offset-md-1">
      <h2>dishes</h2>
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
              <th></th>
              <th>料理画像</th>
              <th>料理名</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @dishes.each do |book| %>
            <tr>
              <td><%=link_to attachment_image_tag(@dishes, :image, :fill, 60, 60, fallback: "no_image.jpeg") %></td>
              <%#=link_to attachment_image_tag(@dishes, :image, :fill, 60, 60, fallback: "no_image.jpeg"), user_path(book.id) %>
              <td><%=link_to dish.title,dish_path(dish.id) %></td>
              <!--<td>-->
                <%#= link_to "#{book.post_comments.count}コメント",book_path(book.id) %>
              <!--</td>-->
            </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
</div>